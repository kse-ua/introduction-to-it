cache = {}

def addProcedure (a, b):
    key = str(a) + ", " + str(b)
    res = 
    if res:
        return res
    res = a + b
    cache[key] = res
    return res

def subProcedure (a, b):
    key = str(a) + ", " + str(b)
    res = 
    if res:
        return res
    res = a - b
    cache[key] = res
    return res

