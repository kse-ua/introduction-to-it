'use strict';

const schoolAges = [10, 12, 15, 15];
const studentAges = [17, 18, 18, 19, 20];

const wholeSet = schoolAges.concat(studentAges);
const first = wholeSet[0];
const last = wholeSet[array.length - 1];

console.log({first, last});
