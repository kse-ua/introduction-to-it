# Implementation

def getFirstAndLast(array): # in JS we use const name  = () =>
  first = ages[0] # the same
  last = ages[-1] # in JS use 'length-1'
  return first, last # the same 

# Usage

ages = [10, 12, 15, 15, 17, 18, 18, 19, 20] # the same
first, last = getFirstAndLast(ages) # the same
print({ 'first': first, 'last': last }) # in JS  use 'console.log'
