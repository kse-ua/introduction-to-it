# Implementation

getFirstAndLast = lambda array: (array[0], array[-1]) # To declare function in JS we use const name  = () =>

# Usage

ages = [10, 12, 15, 15, 17, 18, 18, 19, 20] # This line is the same in JS
first, last = getFirstAndLast(ages) # This line is the same in JS
print({ 'first': first, 'last': last }) # To print elements in JS we use 'console.log'
