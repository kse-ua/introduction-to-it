# Implementation

getFirstAndLast = lambda array: (array[0], array[-1]) # in JS use const name  = () =>


# Usage

ages = [10, 12, 15, 15, 17, 18, 18, 19, 20] # the same
first, last = getFirstAndLast(ages) # the same
print({ 'first': first, 'last': last }) # use 'console.log'
