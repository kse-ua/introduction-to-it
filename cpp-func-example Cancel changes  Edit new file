#include <stdio.h>

void newFunction(int *ages){
    int first = ages[0];
    int length = sizeof(ages);
    int last = ages[length];
    printf("first: %d\n", first);
    printf("last: %d\n", last);
}
int main() {
  int ages[] = { 10, 12, 15, 15, 17, 18, 18, 19, 20 };
  newFunction(ages);
}
